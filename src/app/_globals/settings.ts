export const Settings=Object.freeze({
    BASE_API_URL:'http://localhost:8001',
    APP_NAME:'AQS',//make sure this is unique in your range of apps. should contain no spaces.
    APP_LOGIN_URL:'/login', //for the angular app 
});
